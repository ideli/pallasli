(function($){
	dragJs.dragele.Panel=function(cfg){  
		this.init(cfg);
	};

	$.extend(dragJs.dragele.Panel,dragJs.DragEle, {
		initDragEle:function(srcElement){
			
		},
		initDragStart:function(ev){},
		initDragOver:function(ev){},
		initDragEnd:function(ev){},
		
		
		
		init:function(cfg){
			
		}
	});
})(jQuery);