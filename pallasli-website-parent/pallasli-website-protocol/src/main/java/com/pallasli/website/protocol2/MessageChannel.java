package com.pallasli.website.protocol2;

/**
 * 主要功能都在这个类中，用于发送和接收消息，并封装了所有对于Buffer的操作。
 * 
 * @author lyt1987
 *
 */
class MessageChannel {
	// /**
	// * 构造方法，要求指明发送缓冲区和接收缓冲区的大小
	// */
	// public MessageChannel(int receiveBufferSize, int sendBufferSize,
	// SocketChannel sc, MessageFactory mf);
	//
	// /**
	// * 接收一条消息，当消息不完整、收到的消息长度太大（超过接收缓冲区大小）
	// * 或者不能通过MessageFactory创建的消息类型时时抛出MessageFormatException。
	// */
	// public Message receive() throws IOException, MessageFormatException;
	//
	// /**
	// * 发送一条消息
	// */
	// public void send(Message message) throws IOException;

}
